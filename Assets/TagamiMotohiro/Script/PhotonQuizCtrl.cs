using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Pun;

public class PhotonQuizCtrl : MonoBehaviourPunCallbacks
{
    [Header("正解のオブジェクト一覧")]
    [SerializeField]
    KeyObject[] collectObjects;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       
    }
}
